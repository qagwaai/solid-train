{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "character-bust-update-response",
  "description": "Response after successfully updating a playable-character bust descriptor. Returns the normalized stored descriptor after the update.",
  "type": "object",
  "required": [
    "success",
    "message",
    "correlationId",
    "requestIdentity",
    "playerName",
    "characterId",
    "descriptor"
  ],
  "additionalProperties": false,
  "properties": {
    "success": { "type": "boolean" },
    "message": { "type": "string" },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Echo of the request correlationId."
    },
    "requestIdentity": {
      "type": "object",
      "required": ["operation", "entityType", "containerId"],
      "properties": {
        "operation": { "type": "string" },
        "entityType": { "type": "string" },
        "containerId": { "type": "string" }
      }
    },
    "playerName": { "type": "string" },
    "characterId": {
      "type": "string",
      "description": "The character whose bust was updated."
    },
    "descriptor": {
      "$ref": "./bust-descriptor.schema.json",
      "description": "The normalized stored bust descriptor after the update."
    }
  }
}
