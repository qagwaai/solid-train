{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "market-sell-request",
  "type": "object",
  "required": ["playerName", "sessionKey", "marketId", "solarSystemId", "characterId", "itemId", "quantity"],
  "properties": {
    "playerName": { "type": "string" },
    "sessionKey": { "type": "string" },
    "marketId": { "type": "string" },
    "solarSystemId": { "type": "string" },
    "characterId": { "type": "string" },
    "itemId": {
      "type": "string",
      "description": "Catalog or market inventory item id to sell. The server normalizes matching case-insensitively."
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Requested sell quantity. Must be a positive integer."
    },
    "requestId": {
      "type": "string",
      "description": "Optional caller correlation id echoed in the response."
    },
    "transactionId": {
      "type": "string",
      "description": "Optional caller-supplied transaction id. If omitted, the server creates one."
    }
  }
}
