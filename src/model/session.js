'use strict';

const INVALID_SESSION_EVENT = 'invalid-session';
const INVALID_SESSION_MESSAGE = 'Invalid session';

/**
 * @typedef {Object} InvalidSessionResponse
 * @property {string} message
 */

module.exports = {
  INVALID_SESSION_EVENT,
  INVALID_SESSION_MESSAGE
};
