{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "npc-bust-read-response",
  "description": "Response containing the current stored bust descriptor for an NPC, including the deterministicSeed used at creation.",
  "type": "object",
  "required": [
    "success",
    "message",
    "correlationId",
    "requestIdentity",
    "npcId",
    "deterministicSeed",
    "descriptor"
  ],
  "additionalProperties": false,
  "properties": {
    "success": { "type": "boolean" },
    "message": { "type": "string" },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Echo of the request correlationId."
    },
    "requestIdentity": {
      "type": "object",
      "required": ["operation", "entityType", "containerId"],
      "properties": {
        "operation": { "type": "string" },
        "entityType": { "type": "string" },
        "containerId": { "type": "string" }
      }
    },
    "npcId": {
      "type": "string",
      "description": "The NPC whose bust was read."
    },
    "deterministicSeed": {
      "type": "string",
      "description": "The seed used to generate this NPC bust at creation. Included in read responses for replay and consistency verification."
    },
    "descriptor": {
      "$ref": "./bust-descriptor.schema.json",
      "description": "The normalized stored bust descriptor."
    },
    "appliedOverrides": {
      "type": "array",
      "description": "List of field names that were overridden by admin-tool input at creation or update time.",
      "items": { "type": "string" }
    }
  }
}
