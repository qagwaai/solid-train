{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "celestial-body-upsert-response",
  "type": "object",
  "required": ["success", "message", "playerName", "correlationId", "requestIdentity"],
  "properties": {
    "success": { "type": "boolean" },
    "message": { "type": "string" },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Echo of the request correlationId."
    },
    "requestIdentity": {
      "type": "object",
      "required": ["operation", "entityType", "containerId"],
      "properties": {
        "operation": { "type": "string" },
        "entityType": { "type": "string" },
        "containerId": { "type": "string" }
      }
    },
    "playerName": { "type": "string" },
    "celestialBody": {
      "type": "object",
      "description": "Present on successful celestial body upsert responses.",
      "additionalProperties": true
    }
  }
}
